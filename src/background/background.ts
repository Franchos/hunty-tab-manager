// chrome.runtime.onMessage.addListener((req, sender, sendResponse) => {
//   if (req.type === "getTabs") {
//     chrome.tabs.query({}, (tabs) => {
//       sendResponse(tabs);
//     });
//   }
//   return true;
// });
